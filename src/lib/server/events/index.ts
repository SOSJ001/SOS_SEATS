/**
 * Rebuild server domain: events (roadmap 1.2).
 * Owns: organizer event create/update/load (Kit path).
 * New privileged event helpers land here; do not grow $lib/supabase.js.
 * Do not import from client components.
 */
