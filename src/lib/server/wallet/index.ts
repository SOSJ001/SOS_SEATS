/**
 * Rebuild server domain: wallet (roadmap 1.2).
 * Owns: proceeds / payout Kit helpers (8.x).
 * New privileged wallet helpers land here; do not grow $lib/supabase.js.
 * Do not import from client components.
 */
